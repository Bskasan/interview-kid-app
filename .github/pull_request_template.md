## Checklist

- [ ] `npm run check` passes locally (typecheck, lint, format, tests, build)
- [ ] All user-facing strings exist in both `tr` and `en` resources
- [ ] Internal docs stay private (no references from app code or README)
